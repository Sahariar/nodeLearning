{
  "name": "raw-preoject-1",
  "version": "1.0.0",
  "description": "a simple app for checking url down or up",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "SET NODE_ENV=staging & nodemon index",
    "production": "SET NODE_ENV=production & nodemon index"
  },
  "author": "Sahariar",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.20"
  }
}
